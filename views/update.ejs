<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Book Directory</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="main container d-flex flex-column align-items-center w-100">
      <h1>Update Book</h1>
      <form action="/update/<%= book._id %>" method="post" class="border container d-flex flex-column gap-2 p-2 w-50">
        <label for="title">Book Title</label>
        <input name="title" class="w-30" type="text" placeholder="Book Title" value="<%= book.title %>" />

        <label for="authorName">Author Name</label>
        <input name="authorName" type="text" placeholder="Author Name" value="<%= book.authorName %>" />

        <label for="pages">Number of Pages</label>
        <input name="pages" type="number" placeholder="Pages" value="<%= book.pages %>" />

        <button type="submit" class="btn btn-primary">Update Book</button>
      </form>
    </div>
  </body>
</html>
